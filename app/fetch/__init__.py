"""Fetch-and-hop code for Story 1.2 (LV document download + enrichment).

Deliberately separate from ``app.llm``, which stays parsing/extraction-only.
Nothing in this package makes a network call at import time.
"""
